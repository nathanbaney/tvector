package com.neb.tvector.actions;

import com.neb.tvector.Player;

public class AccelerateForward extends Action {
    public void execute(Player player){
        player.accelerateForward();
    }
}
